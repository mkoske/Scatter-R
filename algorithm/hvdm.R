hvdm <- function(a, b, ranges, nominal = c()) {

    if(length(a) != length(b))
        stop("Lengths of vectors a and b must be equal")

    result <- NULL

    # TODO: implement :)
    
    return(result)
}
